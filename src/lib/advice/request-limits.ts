/** Maximum UTF-8 encoded JSON body accepted by the advisor turn endpoint. */
export const MAX_ADVISOR_BODY_BYTES = 24 * 1024

/** Maximum transcript turns accepted by the advisor turn endpoint. */
export const MAX_ADVISOR_CONVERSATION_TURNS = 12
