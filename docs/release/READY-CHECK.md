# Exact-head ready check

A release candidate is ready only when the exact `main` SHA intended for deployment has clean Quality and Mobile Core checks, the host build completes (including Atlas refresh/verification when route data exists), and the live smoke confirms the restarted service.
