# UX remediation ledger — 2026-08-26

This is the authoritative release ledger for the current Switchback UX wave.
It is a snapshot of the dirty checkout at `feature/route-poster-atlas` (HEAD
`876bb75ef91f2dccc01b03531912a7dc428bda65`), not a claim that another branch,
old screenshot, or historical report is current. Each finding has exactly one
status from this vocabulary:

- `fixed + verified`
- `fixed in another branch`
- `open`
- `external gate`
- `physical proof required`

Current uncommitted work is not called verified. A verification target is
included for every item that still needs evidence. Evidence paths are relative
to the repository unless marked otherwise.

## Ledger

| ID | Finding and current evidence | Status | Verification target / release action |
|---|---|---|---|
| OWNER-01 | Navigation exit from active turn-by-turn ride feels stuck or unclear. Owner field report: `docs/quality/UX_LIVE_TESTING_FINDINGS_2026-08-24.md:18-28`. Current `RideHud`/shell changes are present in `src/components/planner/RideHud.tsx` and `src/app/styles/ride-hud.css`, but the final dirty checkout has no fresh browser journey proving the exit affordance. | `open` | Fresh browser run from Start ride through the exit control, including the confirmation/cancel path, at desktop and 390x844; record screenshot/trace and console result. |
| OWNER-02 | Free Ride has no obvious entry point. Owner field report: `docs/quality/UX_LIVE_TESTING_FINDINGS_2026-08-24.md:32-42`. Relevant surfaces are `src/components/shell/FreeRideHud.tsx`, `src/components/planner/PlannerShell.tsx`, and `tests/e2e/free-ride.spec.ts`; automated coverage does not establish discoverability in the current shell. | `open` | Fresh browser planner-home journey: a first-time rider can identify and open Free Ride without a hidden route or direct URL, then reach the suggestion and guided Ride state. |
| OWNER-03 | Turn directions showed meters instead of feet. Owner field report: `docs/quality/UX_LIVE_TESTING_FINDINGS_2026-08-24.md:46-58`. Unit settings and rider-unit tests now exist in `src/lib/settings/rider-units.ts`, `src/components/planner/RouteComparison.tsx`, and `tests/unit/rider-units.test.ts`; current dirty work has not had a fresh rendered turn-by-turn check. | `open` | Browser route with a short maneuver and a longer maneuver at US/imperial settings; prove feet/miles formatting in the rendered instruction, plus a metric-settings regression check. |
| OWNER-04 / IDENTITY-01 | “Switchback ID” did not work; the field report identifies the live `SWITCHBACK_SESSION_SECRET` as the likely production cause (`docs/quality/UX_LIVE_TESTING_FINDINGS_2026-08-24.md:61-79`). Code fails closed and has missing-secret tests (`src/lib/identity/passkey.ts`, `src/lib/identity/csrf.ts`, `tests/unit/identity-authentication-api.test.ts`, `tests/unit/identity-registration-api.test.ts`), but no live secret/config or real passkey flow is proven here. | `external gate` | Owner/operator must confirm a valid production secret is present without exposing it, then run live registration, verification, session, publish/report, and health checks. Also resolve the separate registration-before-verification finding from `docs/quality/MODULE_SWEEP_REPORT_2026-08-24_sync-identity.md` before claiming identity readiness. |
| OWNER-05 | Route details displayed `0` for road information. Owner field report: `docs/quality/UX_LIVE_TESTING_FINDINGS_2026-08-24.md:83-92`. Current panels use explicit unavailable states in `src/components/planner/RouteDataQualityPanel.tsx` and `src/components/planner/RouteEvidencePanel.tsx`, with focused tests, but no fresh route-detail browser evidence proves the actual data path. | `open` | Load a real route with known road/surface data and one without it; prove the UI shows truthful nonzero data or an explicit unavailable state, never a bare fabricated zero. |
| OWNER-06 | Route detail imagery was generic/fake and unrelated to the route. Owner field report: `docs/quality/UX_LIVE_TESTING_FINDINGS_2026-08-24.md:96-108`. Current `src/components/planner/RouteComparison.tsx` removes generic scenic imagery and the Atlas implementation is geometry-based (`src/lib/gpx/atlas.ts`, `src/lib/gpx/poster.ts`), but the final route/detail/gallery experience is unverified. | `open` | Browser-check route detail and Atlas gallery/detail for route-relevant evidence or a clean no-imagery state; confirm no generic placeholder appears and no missing-geometry path breaks layout. |
| OWNER-07 | Recommended stops were not rider-relevant (requested bakery/coffee/bar quality). Owner field report: `docs/quality/UX_LIVE_TESTING_FINDINGS_2026-08-24.md:112-124`. Place/category fallback work is in `src/lib/geocoding/` and `src/lib/places/` with API/unit coverage, but recommendation quality is not established by the current UI. | `open` | Fresh live/provider-backed or approved fixture journey through a route with stops; verify rider-relevant categories, ranking, empty state, and honest provider failure behavior in the rendered panel. |
| OWNER-08 | “Why this route” leaked raw segment IDs/debug strings. Owner field report: `docs/quality/UX_LIVE_TESTING_FINDINGS_2026-08-24.md:128-144`. Human-readable explanation code/tests exist in `src/lib/recommendation/route-explanations.ts` and `tests/unit/route-explanations.test.ts`; current final rendering still needs evidence. | `open` | Browser route-detail review with normal, unknown-access, and missing-data fixtures; prove user language only, with no internal IDs, debug labels, or misleading certainty. |
| OWNER-09 | Publish/share “sanitized copy” flow was confusing. Owner field report: `docs/quality/UX_LIVE_TESTING_FINDINGS_2026-08-24.md:148-164`. Current copy uses a public-preview model in `src/components/planner/CommunityPublishPanel.tsx` and `src/lib/community/preview-gpx.ts`, but no fresh end-to-end publish/review flow proves the mental model. | `open` | Browser-check review → public preview → publish/cancel, including privacy/coordinate disclosure and failure/retry; verify labels explain what is shared and what remains private. |
| OWNER-10 | Button theming was inconsistent (white controls on black UI). Owner field report: `docs/quality/UX_LIVE_TESTING_FINDINGS_2026-08-24.md:166-178`. Styles changed across `src/app/styles/switchback-v1.css`, `src/app/styles/planner-deck.css`, and route components, but there is no fresh visual review of all named controls. | `open` | Fresh visual QA at desktop, 390x844, and reduced-motion; inspect Save route, GPX format, Export GPX, Start/Edit/Prepare, and modal/secondary controls for contrast, states, and 44px touch targets. |
| CINCO-UX-004 | Dynamic map insets were rejected: the old Phase 1 gate saw identical phone `peek`/`half`/`full` geometry and false-confidence tests. Evidence: `.omo/evidence/cinco-phase-1-gate-review.md:27-31`, `artifacts/cinco/phase-1/responsive-home-phone-portrait.png`. HEAD contains a dynamic implementation in `src/components/planner/workspace/map-viewport-insets.ts`, but current dirty CSS/sheet changes have no fresh map-fit capture. | `open` | Re-run current planner at 390x844 and a desktop target with `peek`, `half`, `full`, and ride states; assert distinct insets and route/map fit from screenshots plus DOM/test assertions. |
| CINCO-UX-003 | Progressive disclosure was rejected because no real user path demonstrated peek → half → full without leaving the map; the visual gate also found half/full non-distinct and no current peek capture. Evidence: `.omo/evidence/cinco-phase-1-gate-review.md:32-39`, `.omo/evidence/cinco-phase-1-visual-gate-review.md:14-16`. Current pointer/tap behavior in `src/components/planner/workspace/ContextSheet.tsx` is uncommitted and prior wave evidence recorded failures in `.omo/evidence/mobile_planner_wave/context-sheet-red.log`. | `open` | Fresh browser pointer-release, tap, keyboard, and drag journey through all detents; prove the rendered sheet heights, map visibility, focus order, and collapse path at target widths. |
| CINCO-TELEMETRY | Phase 1 mobile ride telemetry was clipped at 390x844, while the snapshot encoded the defect. Evidence: `.omo/evidence/cinco-phase-1-gate-review.md:40-46`, `artifacts/cinco/phase-1/ride--mobile.png`. Current ride CSS contains containment changes, but no fresh screenshot/assertion proves the final layout. | `open` | Fresh 390x844 ride and off-route captures with long telemetry/status values; assert no horizontal overflow or clipping and verify readable recovery controls. |
| CINCO-RIDE-003 | Street/Bike profile pill had nearly white text on white in ride and off-route mobile. Evidence: `.omo/evidence/cinco-phase-1-visual-gate-review.md:10-12,50-51`, old source references `src/app/styles/ride-hud.css` and `src/app/styles/switchback-v1.css`. Current CSS appears changed, but the contrast/readability claim is not freshly proven. | `open` | Current browser screenshots plus quantitative contrast check for Street/Bike pills in ride, off-route, hover/focus, and disabled states at mobile width. |
| CINCO-UX-001 | Map-first default was rejected: the old 390x844 default expanded sheet dominated the viewport and obscured the map. Evidence: `.omo/evidence/cinco-phase-1-visual-gate-review.md:18-20`, `.omo/evidence/cinco-phase-1-gate-review.md:17`. Current map-first/sheet work is partial and unverified on the final dirty tree. | `open` | Fresh planner-home screenshot at 390x844 and desktop: map must remain the primary surface, with the intended initial detent, usable controls, and no clipped route. |
| CINCO-UX-002 | Unified context surface is a Phase 1 requirement: planning/detail content must use one coherent ContextSheet/workspace-panel model rather than overlapping drawers. Requirement: `docs/cinco/roadmap/05_REQUIREMENT_CATALOG.md:10-11`; gate architecture references `MapWorkspace` and `ContextSheet`, but no current interaction audit proves every planning/detail path uses the same surface. | `open` | Fresh browser audit of planner, route detail, alternatives, ride transition, and Atlas links; verify one coherent sheet/panel ownership, no stacked independent drawers, and predictable back/close behavior. |
| CINCO-UX-005 | Responsive composition must be intentionally different where needed across phone portrait/landscape, tablet portrait/landscape, and desktop. Requirement: `docs/cinco/roadmap/05_REQUIREMENT_CATALOG.md:19-20`; historical PNG coverage exists, but the current dirty CSS/Atlas wave has no fresh responsive matrix. | `open` | Re-run named viewport matrix (390x844, 844x390, 768x1024, 1024x768, 1440x900) and inspect map, sheet, route detail, and Atlas for clipping, stretching, and breakpoint-specific affordances. |
| CINCO-UX-006 | Primary touch controls must meet the 44x44 CSS pixel floor. Requirement: `docs/cinco/roadmap/05_REQUIREMENT_CATALOG.md:22-23`; prior gate found 44px peek actions but static screenshots cannot establish all current controls. | `open` | Browser/DOM audit of every primary planner, sheet, ride, route-detail, Atlas, and Profile control at mobile width; assert computed dimensions, spacing, keyboard focus, and no clipped hit areas. |
| RELEASE-START-NEW | Minimized mobile planner needs a persistent “Start new route” reset affordance. The implementation/test lives in linked worktree `/root/Vibe/switchback-oxalpha-snr`, branch `ux/owner-findings-2026-08-24`, commit `22d9bcd`; it is not merged or reconciled into this checkout, and that worktree is dirty. | `fixed in another branch` | Review the linked branch diff and its `tests/e2e/critical/snr-peek-proof.spec.ts`, reconcile against current `feature/route-poster-atlas`, then run fresh browser proof before merge. Do not delete/prune the branch or worktree. |
| ATLAS-ROLE | Route Atlas must remain a secondary Library collection/value-add, not replace the map-first planner or claim live navigation/current conditions/full offline readiness. Contract: `DESIGN.md` and `docs/superpowers/specs/2026-08-26-map-first-rider-session-ux-design.md`; implementation: `src/lib/gpx/atlas.ts`, `src/lib/gpx/poster.ts`, `src/app/gpx-library/`, `src/app/gpx-library/[routeId]/`. Focused Atlas tests/typecheck are recorded in `.omo/evidence/atlas-ux-wave/04-green-focused-tests-final.log` and `05-typecheck-final.log`, but route/browser QA is absent. | `open` | Fresh browser gallery/detail/empty/missing-geometry checks at named desktop/mobile widths; verify route-specific geometry, no horizontal overflow, accessible labels, and clear links back to planner without navigation/live/offline overclaim. |
| TEST-DISCOVERY | Vitest discovered tests inside linked `.claude/worktrees`, causing invalid-hook errors and false failures; prior evidence: `.omo/evidence/mobile_planner_wave/planner-deck-red.log` and `planner-deck-red-isolated.log`. A dirty exclusion was added to `vitest.config.ts`, but no fresh root-suite proof exists. | `open` | From repository root run the full test command and focused suites; verify no linked-worktree test paths, invalid-hook warnings, or accidental duplicate test discovery, then preserve a clean log as release evidence. |
| RECONCILIATION | Stale/diverged local branches and linked worktrees make branch/PR state non-authoritative. `PLAN_TONIGHT.md`, `git worktree list`, and `git branch -vv` show multiple dirty or ahead/behind quality-sweep worktrees. No branch deletion, pruning, reset, or merge is authorized by this ledger. | `open` | Inventory every worktree/branch/remote SHA, dirty path, merge-base, and PR; compare each claimed fix to this current checkout, preserve user changes, and document an explicit merge/reconciliation decision. |
| OFFLINE-GRAPH | Saved-route offline packs cannot reliably supply a routing graph: `RegionDownloadClient.getGraph()` reads the legacy Dexie `graphs` table while the active v2 tile path has no production graph writer. Evidence: `src/lib/storage/region-download-client.ts:105-125,329-366`, `src/lib/client/offline-pack-coordinator.ts:88-106`, and `docs/quality/MODULE_SWEEP_REPORT_2026-08-24_offline-workers.md:83-88`. | `open` | Trace one downloaded region from install through `getGraph()` and offline reroute on a clean browser profile; either wire and test a real writer/reader path or expose a truthful unavailable state without synthetic geometry. |
| OFFLINE-PARITY | Generated PA/NJ offline route parity is 187/208 (89.9%), below the release gate; legality is clean and oracle errors are zero, but the gate remains open. Evidence: `docs/phase-reports/P29-offline-geo-worker.md:27-42`, `docs/phase-reports/P30-region-ride-packs.md:19-34`, `docs/quality/RELEASE_EVIDENCE.md:14-20`. | `external gate` | Owner/operator reruns the real GraphHopper/data parity audit with current artifacts and reaches the declared threshold, or records an approved release decision and truthful user-facing limitation. |
| PHYSICAL-IPHONE | Real iPhone Safari/PWA and airplane-mode behavior is not proven. The required sequence is `docs/quality/PHYSICAL_DEVICE_DRILL.md:17-36`; current result is explicitly `NOT PERFORMED` in `docs/quality/PHYSICAL_DEVICE_RESULTS.md:3-13`. | `physical proof required` | Execute the complete online/save/reload/Library/Free Ride/airplane-mode/reconnect drill on the target iPhone/PWA; record device/iOS/revision/network and screenshots without private coordinates. |
| PROFILE-DESKTOP | Profile/Offline-regions content was reported mostly invisible/off-canvas at 1280x900; evidence and recommendation are in `docs/SHAREABILITY-REVIEW-2026-08-14.md:102-114,253`. Existing profile screenshot is historical and the current branch has no fresh browser check. | `open` | Fresh 1280x900 and mobile browser review of Profile, Offline regions, Sync, and recovery controls; prove content is visible, reachable, keyboard/focus usable, and not merely represented by floating buttons. |
| EVIDENCE-COVERAGE | Release evidence still has mobile/CJK/quantitative accessibility and route/ride/profile coverage gaps; the CINCO review calls out no CJK fixture and no current quantitative contrast report (`.omo/evidence/cinco-phase-1-gate-review.md:42-43,96`, `.omo/evidence/cinco-phase-1-visual-gate-review.md:27-28`), while release evidence labels browser/device work pending (`docs/quality/RELEASE_EVIDENCE.md:3-5,19-20`). | `open` | Produce fresh current-HEAD visual/browser evidence for planner, ride, route detail, Atlas, Profile, keyboard/focus, reduced-motion, CJK fixture, and contrast; attach logs/screenshots to the release evidence index. |

## Coverage and unresolved categories

Coverage is 27 distinct ledger entries: all 10 owner comments from
`UX_LIVE_TESTING_FINDINGS_2026-08-24.md`, the five CINCO gate blockers/notes
plus three additional requirement rows (UX-002, UX-005, UX-006), together
covering UX-001..006, the current-wave Start-new-route release finding, Atlas
role, linked-worktree test discovery, stale branch/worktree reconciliation,
offline graph behavior, offline parity, live identity secret, physical
iPhone/PWA, Profile desktop, and evidence/accessibility coverage.

Status counts: `fixed + verified` 0; `fixed in another branch` 1; `open` 23;
`external gate` 2; `physical proof required` 1. The unresolved release
categories are therefore current browser/visual evidence, owner UX behavior,
branch/worktree reconciliation, offline implementation/parity, production
identity configuration, and physical-device qualification. No branch was
deleted or pruned, and no current partial change is represented as verified.
