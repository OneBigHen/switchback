# A11 RED probe

Temporary audit branch used only to execute the new provider-resolved stop-visit contract against PR #109 before production implementation.

Expected RED: `tests/unit/advisor-stop-route-proof.test.ts` references the intended evidence-boundary API before it exists. This branch is not a product candidate and must not be merged.
